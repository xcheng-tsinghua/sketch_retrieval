
cd /opt/data/private/networks/sketch_retrieval/ && conda activate dp

git status && git add . && git commit -m "change" && git push


# 对于目录
# 需要将 ${directory} 更换为已被Git同步但是需要解除同步的文件夹
git rm --cached -r ${directory}

# 对于文件
# 需要将 ${file} 更换为已被Git同步但是需要解除同步的文件
git rm --cached ${file}

# 训练除sdgraph外其他的模型
python train.py --root_sever=/opt/data/private/data_set/sketch_retrieval/qmul_v2_fit/shoe --add_str=shoe --sketch_model=bidir_lstm && python train.py --root_sever=/opt/data/private/data_set/sketch_retrieval/qmul_v2_fit/shoe --add_str=shoe --sketch_model=vit && python train.py --root_sever=/opt/data/private/data_set/sketch_retrieval/qmul_v2_fit/chair --add_str=chair --sketch_model=bidir_lstm && python train.py --root_sever=/opt/data/private/data_set/sketch_retrieval/qmul_v2_fit/chair --add_str=chair --sketch_model=vit
